# Optional: enrich upcoming events with betting odds.
ODDS_API_KEY=

# Optional: enables POST /api/refresh. Send this value in X-Refresh-Token.
# Leave unset in production unless remote cache refreshes are required.
UFC_ELO_REFRESH_TOKEN=
